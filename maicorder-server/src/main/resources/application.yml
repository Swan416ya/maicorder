spring:
  datasource:
    url: jdbc:mysql://localhost:3306/maicorder?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  # MyBatis Plus ??
  mybatis-plus:
    configuration:
      # SQL ????
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
      # ???????? (?? arcade_name -> arcadeName)
      map-underscore-to-camel-case: true

  # Jackson ??
  jackson:
    property-naming-strategy: SNAKE_CASE

# ????
server:
  port: 8007

# ?????
maimaipy-base-url: http://localhost:8000


